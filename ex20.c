#include <stdio.h>

int perimetro (int a, int b, int c)
{
    int perimetro;
    perimetro = a + b + c;
    return perimetro;
}
int main ()
{
    int risultato = perimetro (4, 7, 9);
    scanf("%d %d %d", &a &b &c);
    printf("%d", risultato);
}